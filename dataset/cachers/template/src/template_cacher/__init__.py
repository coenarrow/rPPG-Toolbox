"""Template cacher: raw recordings in, contract-conformant zarr stores out.

Copy this package, rename it after your dataset, and fill in the four
functions and three constants the README lists. Everything else -- the zarr
schema, compression, the alignment arithmetic, skip/rebuild logic, the CLI
and the parallel runner -- is dataset-agnostic and stays as it is.
"""

__version__ = "0.1.0"
